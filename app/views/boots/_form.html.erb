<%= form_with(model: boot) do |form| %>
  <% if boot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boot.errors.count, "error") %> prohibited this boot from being saved:</h2>

      <ul>
        <% boot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
